## 2026-04-23 chg-20260423-1549-3mk

Commit message: feat(logging): add narrative logs to claude code [chg-20260423-1549-3mk]

Summary: Implements a narrative logging system modeled on the reelweaver project. Every agent turn now produces a human-readable Markdown log (NARRATIVE.md + per-turn snapshots) and complete raw Anthropic API wire-format request/response JSON files in `~/.claude/narrative-logs/<workspace>/<session>/`. The implementation is purely additive — core agent loop logic is untouched, all hooks use optional chaining so a null logger is a zero-cost no-op. Controlled by the NARRATIVE_LOGGING env var (default: on).

Files:
- claude-code-source/src/utils/narrativeLogger.ts (new) — FileNarrativeLogger class, NarrativeLogger interface, createNarrativeLoggerFromEnv factory
- claude-code-source/src/utils/narrativeSession.ts (new) — module-level singleton accessor, process.beforeExit handler for session analytics
- claude-code-source/src/query.ts (modified) — import + 20 call sites: startTurn, logModelRequest, logModelRound, logToolResult, finishTurn before every return path
- claude-code-source/src/services/api/claude.ts (modified) — writeRawAPIRequest (before anthropic.beta.messages.create) + writeRawAPIResponse (after streaming)

Notes: No capsule declared. No durable rules files (failure-memory.md, signs.md, patterns.md, specs.md) were modified in this session. Build verified clean (pnpm build, 23.2MB output). Log directory structure mirrors reelweaver for direct side-by-side comparison: NARRATIVE.md, turns/turn-NNN.md, requests/turn-NNN-round-NNN-{request,response}.json.
