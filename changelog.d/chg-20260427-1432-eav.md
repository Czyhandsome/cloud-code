## 2026-04-27 chg-20260427-1432-eav

Commit message: feat(narrative): expose narrative session dir in logo prompt [chg-20260427-1432-eav]

Summary: Added a `getNarrativeDir()` export to `narrativeSession.ts` that returns the
current session's narrative log directory path without initialising the logger. Wired this
into `getLogoDisplayData()` and rendered a `Session: <path>` notice below the logo box in
all three LogoV2 layout paths (condensed, compact, full horizontal), matching the style of
the existing debug-mode and TMUX notices. Also migrated the session-dir path computation to
use `getClaudeConfigHomeDir()` (respects `CLAUDE_CONFIG_DIR`) instead of a hardcoded
`~/.claude2` join, and extracted it into a separately-memoised `computeNarrativeDir()`
helper so both the display and the logger share the exact same path.

Files:
- claude-code-source/src/utils/narrativeSession.ts — extracted `computeNarrativeDir()`, added `getNarrativeDir()` export, reset `_sessionDir` in `resetNarrativeLogger()`
- claude-code-source/src/utils/logoV2Utils.ts — imported `getNarrativeDir`, added `narrativeDir` field to `getLogoDisplayData()` return type and value
- claude-code-source/src/components/LogoV2/LogoV2.tsx — destructured `narrativeDir`, appended `Session:` notice in all three code-path returns

Notes: No changes to `_c(N)` React Compiler cache allocation or existing memoisation
indices — the new element is added outside all memoised logo boxes, consistent with how
debug-mode and TMUX info are already rendered.
