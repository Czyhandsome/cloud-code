## 2026-04-24 chg-20260424-0947-od0

Commit message: feat(config): redirect claude2 user config from ~/.claude to ~/.claude2 [chg-20260424-0947-od0]

Summary: Changed the default user-home config directory for the claude2 binary from `~/.claude` to `~/.claude2`, achieving full isolation between the real `claude` and the locally-built `claude2`. Also routed the auth credential file (`~/.claude.json`) through the same config dir, and updated the permission pattern constant accordingly.

Files:
- claude-code-source/src/utils/envUtils.ts — changed `getClaudeConfigHomeDir()` default from `.claude` to `.claude2`
- claude-code-source/src/utils/env.ts — changed `getGlobalClaudeFile()` to route through `getClaudeConfigHomeDir()` instead of falling back to raw `homedir()`, so the auth JSON (`~/.claude.json`) now lands in `~/.claude2/`
- claude-code-source/src/tools/FileEditTool/constants.ts — updated `GLOBAL_CLAUDE_FOLDER_PERMISSION_PATTERN` from `~/.claude/**` to `~/.claude2/**`

Notes: Only user-home-level paths were changed; project-level `.claude/` directories are out of scope. The `CLAUDE_CONFIG_DIR` env var still works as an override. All 30+ subdirectories under `~/.claude/` are centralized through `getClaudeConfigHomeDir()` and move automatically. The one edge case was `getGlobalClaudeFile()` which had a separate `homedir()` fallback — now fixed to be consistent. Dist was rebuilt from source (`bun run build`) and reinstalled via `make install`.
