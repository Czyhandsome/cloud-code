## 2026-04-24 chg-20260424-1405-0nb

Commit message: fix(narrative): redirect narrative logs from .claude to .claude2 [chg-20260424-1405-0nb]

Summary: Narrative session logs were being written to `~/.claude/narrative-logs/` instead of `~/.claude2/narrative-logs/`. Fixed the hardcoded `.claude` path in `narrativeSession.ts` and rebuilt + reinstalled the `claude2` binary.

Files:
- claude-code-source/src/utils/narrativeSession.ts

Notes: No plan involved — single-line path fix. Binary reinstalled via `make install` (v2.1.88). User will manually migrate existing NARRATIVES from `~/.claude` to `~/.claude2`.
