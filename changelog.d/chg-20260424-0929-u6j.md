## 2026-04-24 chg-20260424-0929-u6j

Commit message: build(install): add Makefile for claude2 binary install [chg-20260424-0929-u6j]

Summary: Adds a root-level Makefile with a `make install` target that compiles the claude-code-source into a standalone Mach-O binary named `claude2` using `bun build --compile`, then installs it at `~/.local/share/claude2/versions/<version>` with a symlink at `~/.local/bin/claude2`, mirroring the installation pattern of the real `claude` binary.

Files:
- Makefile (new)

Notes: The `sharp` image-processing module is marked `--external` during compilation because it is a dynamic import in the already-bundled dist/cli.js that bun cannot resolve at compile time. This only affects image-resize codepaths, which are not exercised in normal CLI usage.
