## 2026-04-24 chg-20260424-1027-les

Commit message: fix(color-diff): import TS port directly to fix render crash [chg-20260424-1027-les]

Summary: The `ColorDiff` class imported via the `color-diff-napi` stub package had wrong method names (`diff()`/`getColors()`) instead of the required `render()`, causing a hard crash whenever StructuredDiff tried to render a colored hunk. Fixed by redirecting the import in `colorDiff.ts` to the pure-TS port at `src/native-ts/color-diff/index.ts`, which implements `render()` correctly and is already bundled.

Files:
- claude-code-source/src/components/StructuredDiff/colorDiff.ts

Notes: The `stubs/color-diff-napi` package is now dead weight for this consumer — only `colorDiff.ts` imported it, and that import is gone. The stub remains in place for any future native-module path, but the TS port is now the active implementation.
